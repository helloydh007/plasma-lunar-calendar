<?xml version="1.0" encoding="UTF-8"?>
<kcfg xmlns="http://www.kde.org/standards/kcfg/1.0"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xsi:schemaLocation="http://www.kde.org/standards/kcfg/1.0
      http://www.kde.org/standards/kcfg/1.0/kcfg.xsd" >
  <kcfgfile name=""/>

  <group name="General">
    <!-- 是否在月历左侧显示自定义「学期周数」列 -->
    <entry name="showTermWeeks" type="Bool">
      <default>false</default>
    </entry>
    <!-- 第 1 周的起始日，ISO 格式 yyyy-MM-dd；空表示未设置 -->
    <entry name="termStart" type="String">
      <default></default>
    </entry>
    <!-- 是否在日期格子上标记法定节假日（休）与调休上班日（班） -->
    <entry name="showHolidays" type="Bool">
      <default>true</default>
    </entry>
    <!-- 节假日数据更新方式：manual（手动，默认）| auto（自动，每 30 天检查一次） -->
    <entry name="holidayUpdateMode" type="String">
      <default>manual</default>
    </entry>
    <!-- 联网获取到的节假日数据缓存（JSON；只保存内置数据未覆盖的年份） -->
    <entry name="holidayCache" type="String">
      <default></default>
    </entry>
    <!-- 桌面上的显示样式：
         month（整月网格，默认）| today（今日）| week（本周）
         | mini（迷你月历 + 今日）| upcoming（假期倒计时）| almanac（农历详情）
         认不出来的值一律退回 month，配置被手改坏时不会什么都不显示。
         面板（任务栏）里不用这个，固定显示一行紧凑视图。 -->
    <entry name="viewStyle" type="String">
      <default>month</default>
    </entry>
    <!-- 背景不透明度（0-100）。0 = 完全没有底板，只剩内容浮在桌面上。
         背景由组件自己绘制，所以这里的数值是准的。 -->
    <entry name="backgroundOpacity" type="Int">
      <default>100</default>
    </entry>
    <!-- 卡片底色的不透明度（0-100）。0 = 卡片底色完全透明，
         只剩文字与细描边；文字始终不透明。 -->
    <entry name="cardOpacity" type="Int">
      <default>100</default>
    </entry>
  </group>
</kcfg>
